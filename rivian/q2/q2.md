geocoding

map coordinates to road segments or weather zones

The Task: "You are merging GPS logs with a 'Service Center' table to find which vehicles are within 50 miles of a Rivian shop. 

The GPS data is noisy (some nulls, some impossible speeds)."

The Focus: Data cleaning and filtering.

The Setup

Input 1: telemetry_logs (Billions of rows: vehicle_id, ts, lat, lng, speed_mph)

Input 2: service_centers (Small lookup table: center_id, center_lat, center_lng)

Goal: Find vehicles within 50 miles of a center, while filtering "trash" data.

